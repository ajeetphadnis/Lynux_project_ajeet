<!DOCTYPE html>
<html>

<head>
	<meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
	<!--link rel="stylesheet" href="/css/pha_styles001.css"  type="text/css"-->
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link rel="stylesheet"	href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
 		<style type="text/css">
		.container i {
			color: lightblue;
		    margin-left: -30px;
		    cursor: pointer;
		}
		
		h2, h3, h4 {
			color: #2B547E;
			font-family: Verdana;
			font-size: 15px;
		}
				
		.parent {
			text-align: center;
		}
		.parent > ul {
		  display: inline-block;
		}
		
		
		body {
			background-color: #BCC6CC;
		}
	</style>
</head>
	<title> Authentication Token Converter Middleware Services </title>
	<script>
		toggleEye = function(  ) {
	  		const togglePassword = document.querySelector('#togglePassword');
			const password = document.querySelector('#password');
			togglePassword.addEventListener('click', function (e) {
		    // toggle the type attribute
		    const type = password.getAttribute('type') === 'password' ? 'text' : 'password';
		    password.setAttribute('type', type);
		    // toggle the eye slash icon
		    this.classList.toggle('fa-eye-slash');
			});
		}
	</script>


<body >
	<center>
<h2> Authentication Token Converter Middleware Services </h2>
<h3>
The Authentication Token Converter Middleware is a way to establish 	<br>
trust between parties that want to use online identities of one another.<br>
To demo this service, the user is first navigated to login, if user has already<br>
a user id in our system, then user is logged in and navigated to demo.<br>
Else the user is prompted to go to registration page,<br>
and then the user is navigated to login page, where after <br>
a successful login the user is navigated to Converter Middleware<br>
service demo page.<br>

</h3>


		<form style="max-width:250px;margin:auto" action="/login_user" method="POST">
			<div class="container">
				<input type="text" name="nameIdentifier" id="nameIdentifier" required placeholder="name Identifier">
				<i class="fa fa-user icon"></i>
			</div>
			<div class="container">
        		<input type="password" name="password" id="password" required placeholder="password">
        		<i class="fa fa-eye icon" id="togglePassword" name="togglePassword" onClick="toggleEye()"></i>        		
    		</div>
 			<button type="submit" class="btn btn-primary">Submit</button>
		</form>
		<form>
			<div class="parent">
				<button formaction="/register_user">Register</button>
        	</div>
        	<div class="parent">
				<button formaction="/protocolconvert_start">App Overview</button><br>
	        </div>
		</form>
	</center>
</body>
</html>
